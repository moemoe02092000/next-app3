'use client';
import React from "react";

const AddToCart = () => {
  return (
    <div>
      <button className="btn btn-outline btn-xl btn-wide glass btn-success" onClick={() => console.log("clicked")}>AddToCart</button>
    </div>
  );
};

export default AddToCart;
